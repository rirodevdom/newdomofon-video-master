name: Archive self-update script

on:
  pull_request:
    paths:
      - 'update-installed-project.sh'
      - 'scripts/install-master-from-archive.sh'
      - 'docs/UPDATE_FROM_ARCHIVE.md'
      - '.github/workflows/archive-self-update.yml'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate shell syntax and help
        run: |
          bash -n update-installed-project.sh
          bash -n scripts/install-master-from-archive.sh
          bash update-installed-project.sh --help >/tmp/archive-updater-help.txt
          bash scripts/install-master-from-archive.sh --help >/tmp/archive-installer-help.txt
          grep -q 'NewDomofon Video Master' /tmp/archive-updater-help.txt
          grep -q -- '--dry-run' /tmp/archive-updater-help.txt
          grep -q 'без Git' /tmp/archive-installer-help.txt

      - name: Reject Git commands in server archive operations
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          files = [
              Path('update-installed-project.sh'),
              Path('scripts/install-master-from-archive.sh'),
              Path('docs/UPDATE_FROM_ARCHIVE.md'),
          ]
          forbidden = [
              re.compile(r'(^|[;&|])\s*(sudo\s+)?git(\s|$)', re.IGNORECASE),
              re.compile(r'\bgit\s+(clone|fetch|pull|switch|checkout|reset|init|status|diff|rev-parse|remote|add|commit)\b', re.IGNORECASE),
              re.compile(r'\b(command\s+-v|which)\s+git\b', re.IGNORECASE),
              re.compile(r'\bapt(?:-get)?\s+install[^\n]*\bgit\b', re.IGNORECASE),
          ]

          violations = []
          for path in files:
              for number, line in enumerate(path.read_text(encoding='utf-8').splitlines(), 1):
                  if line.lstrip().startswith('#'):
                      continue
                  if any(pattern.search(line) for pattern in forbidden):
                      violations.append(f'{path}:{number}: {line}')

          if violations:
              raise SystemExit('Git commands are forbidden in server install/update paths:\n' + '\n'.join(violations))
          PY

      - name: Exercise extracted-archive dry run
        run: |
          install -d "$RUNNER_TEMP/installed-master/scripts"
          cp scripts/deploy-master.sh "$RUNNER_TEMP/installed-master/scripts/deploy-master.sh"

          bash update-installed-project.sh \
            --project-dir "$RUNNER_TEMP/installed-master" \
            --dry-run \
            > /tmp/archive-updater-dry-run.txt

          grep -q 'DRY RUN завершён' /tmp/archive-updater-dry-run.txt
          grep -q 'Синхронизация файлов архива' /tmp/archive-updater-dry-run.txt
          grep -q 'SHA-256 содержимого' /tmp/archive-updater-dry-run.txt

      - name: Validate safety markers
        run: |
          grep -q 'pg_dump -Fc' update-installed-project.sh
          grep -q 'deploy-master-preserve-nginx.sh' update-installed-project.sh
          grep -q 'repair-public-media-cors.sh' update-installed-project.sh
          grep -q 'newdomofon-master-archive-update.lock' update-installed-project.sh
          grep -q 'source_fingerprint_sha256' update-installed-project.sh
          grep -q 'repository_access_used=false' update-installed-project.sh
          grep -q 'install-master-local-root.sh' scripts/install-master-from-archive.sh
          grep -q -- '--exclude=node_modules/' update-installed-project.sh
          grep -q -- '--exclude=dist/' update-installed-project.sh
